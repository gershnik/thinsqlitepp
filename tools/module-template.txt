/*
 Copyright 2019 Eugene Gershnik

 Use of this source code is governed by a BSD-style
 license that can be found in the LICENSE file or at
 https://github.com/gershnik/thinsqlitepp/blob/master/LICENSE
*/
module;

#ifndef SQLITE_VERSION
    #if defined(THINSQLITEPP_CUSTOM_SQLITE_H)
        #include THINSQLITEPP_CUSTOM_SQLITE_H
    #elif THINSQLITEPP_BUILDING_EXTENSION
        #include <sqlite3ext.h>
        SQLITE_EXTENSION_INIT3
    #else
        #include <sqlite3.h>
    #endif
#endif

##SYS_INCLUDES##

//The standard allows declarations here but
//GCC and MSVC being hostile and moronic decide to warn about it.
//There are very legitimate reasons to declare things here
#if defined(__GNUC__) && !defined(__clang__)
    //With GCC 16 this currently generates the very warning it is supposed 
    //to suppress. That's hostile on a whole another level.
    #pragma GCC diagnostic ignored "-Wglobal-module"
#elif defined(_MSC_VER)
    //"a global module fragment can only contain preprocessor directives"
    //at least they let you disable it
    #pragma warning(disable: 5202)
#endif

// forward declare it so at least the declaration is there 
// whether <sys/uio.h> is present and whether it declares it
struct iovec; 

export module thinsqlitepp;

#define SQLITEPP_BUILDING_MODULE 1 

##CONTENT##
